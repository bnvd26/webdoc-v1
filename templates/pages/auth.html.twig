{% extends 'base.html.twig' %}
{% block body %}


<h1>Connexion</h1>

<form action="{{ path('admin.connexion') }}" method="post">
    <div class="form-group">
    <input placeholder="Adresse mail" required name="_username" type="text" class="form-control">
    </div>
    <div class="form-group">
    <input placeholder="Mot de passse" required name="_password" type="password" class="form-control">
    </div>
    <div class="form-group">
    <button  class="btn btn-success" type="submit">Connexion</button>
    </div>

</form>
{% endblock %}