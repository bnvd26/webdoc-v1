
    {% extends 'base.html.twig' %}
        {% block body %}
      {% if app.user %}
   <h2>CHAPTER ONE</h2>
          {% for details in chapterOne %}

          <p>{{details.id}}</p>
          <a href={{ path('admin.edit.one', {id: details.id}) }}>Editer</a>
          <form method="post" action="{{ path('admin.delete.one', {id: details.id}) }}">
          <input type="hidden" name="_method" value="DELETE">
          <button>Supprimer</button>
          </form>
          
          {% endfor %}
          <a href={{ path('admin.create.one')}}>Creer un details</a>
    <h2>CHAPTER TWO</h2>
             {% for detail in chapterTwo %}
             <p>{{detail.id}}</p>
             <a href={{ path('admin.edit.two', {id: detail.id}) }}>Editer</a>
          <form method="post" action="{{ path('admin.delete.two', {id: detail.id}) }}">
          <input type="hidden" name="_method" value="DELETE">
          <button>Supprimer</button>

      
          {% endfor %}
          <a href={{ path('admin.create.two')}}>Creer un details</a>
        
        {% else %}
        <h3>ERREUR</h3>
        <p>Il faut d'abord vous connectez pour acceder a cette partie de l'application</p>
        <a href={{ path('admin.connexion') }}>Se connecter</a>
        {% endif %}
        {% endblock %}
        {% block javascripts %}
         {{ encore_entry_script_tags('app') }}
        
        {% endblock %}
  
